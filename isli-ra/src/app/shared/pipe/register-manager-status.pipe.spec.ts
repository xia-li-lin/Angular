import { RegisterManagerStatusPipe } from './register-manager-status.pipe';

describe('RegisterManagerStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterManagerStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
