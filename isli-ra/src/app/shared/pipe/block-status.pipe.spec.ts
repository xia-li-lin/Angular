import { BlockStatusPipe } from './block-status.pipe';

describe('BlockStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
