import { GameTypeTransPipe } from './game-type-trans.pipe';

describe('GameTypeTransPipe', () => {
  it('create an instance', () => {
    const pipe = new GameTypeTransPipe();
    expect(pipe).toBeTruthy();
  });
});
