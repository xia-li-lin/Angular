import { GameStatusTransPipe } from './game-status-trans.pipe';

describe('GameStatusTransPipe', () => {
  it('create an instance', () => {
    const pipe = new GameStatusTransPipe();
    expect(pipe).toBeTruthy();
  });
});
