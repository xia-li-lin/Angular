import { UnitConverPipe } from './unit-conver.pipe';

describe('UnitConverPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitConverPipe();
    expect(pipe).toBeTruthy();
  });
});
