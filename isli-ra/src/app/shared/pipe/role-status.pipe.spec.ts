import { RoleStatusPipe } from './role-status.pipe';

describe('RoleStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
