import { AuditResultPipe } from './audit-result.pipe';

describe('AuditResultPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditResultPipe();
    expect(pipe).toBeTruthy();
  });
});
