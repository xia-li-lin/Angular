import { ShowHideTransPipe } from './show-hide-trans.pipe';

describe('ShowHideTransPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowHideTransPipe();
    expect(pipe).toBeTruthy();
  });
});
