import { GameProcessDetailsTransPipe } from './game-process-details-trans.pipe';

describe('GameProcessDetailsTransPipe', () => {
  it('create an instance', () => {
    const pipe = new GameProcessDetailsTransPipe();
    expect(pipe).toBeTruthy();
  });
});
