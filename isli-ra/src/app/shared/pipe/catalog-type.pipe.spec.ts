import { CatalogTypePipe } from './catalog-type.pipe';

describe('CatalogTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogTypePipe();
    expect(pipe).toBeTruthy();
  });
});
