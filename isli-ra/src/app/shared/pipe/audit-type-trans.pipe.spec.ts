import { AuditTypeTransPipe } from './audit-type-trans.pipe';

describe('AuditTypeTransPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditTypeTransPipe();
    expect(pipe).toBeTruthy();
  });
});
