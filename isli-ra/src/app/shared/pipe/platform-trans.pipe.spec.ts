import { PlatformTransPipe } from './platform-trans.pipe';

describe('PlatformTransPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformTransPipe();
    expect(pipe).toBeTruthy();
  });
});
