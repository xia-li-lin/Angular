import { TemplateStatusDirective } from './template-status.directive';

describe('TemplateStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateStatusDirective();
    expect(directive).toBeTruthy();
  });
});
