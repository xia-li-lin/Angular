import { PwdFormatDirective } from './pwd-format.directive';

describe('PwdFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdFormatDirective();
    expect(directive).toBeTruthy();
  });
});
