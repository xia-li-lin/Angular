import { ValidJobNoDirective } from './valid-job-no.directive';

describe('ValidJobNoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidJobNoDirective();
    expect(directive).toBeTruthy();
  });
});
