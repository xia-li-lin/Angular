import { JobNoExistDirective } from './job-no-exist.directive';

describe('JobNoExistDirective', () => {
  it('should create an instance', () => {
    const directive = new JobNoExistDirective();
    expect(directive).toBeTruthy();
  });
});
