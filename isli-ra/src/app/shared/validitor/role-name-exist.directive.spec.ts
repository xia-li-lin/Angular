import { RoleNameExistDirective } from './role-name-exist.directive';

describe('RoleNameExistDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleNameExistDirective();
    expect(directive).toBeTruthy();
  });
});
