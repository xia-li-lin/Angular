import { ValidNameUniqueDirective } from './valid-name-unique.directive';

describe('ValidNameUniqueDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidNameUniqueDirective();
    expect(directive).toBeTruthy();
  });
});
