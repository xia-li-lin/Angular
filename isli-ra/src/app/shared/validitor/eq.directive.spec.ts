import { EqDirective } from './eq.directive';

describe('EqDirective', () => {
  it('should create an instance', () => {
    const directive = new EqDirective();
    expect(directive).toBeTruthy();
  });
});
