import { VaileMobileDirective } from './vaile-mobile.directive';

describe('VaileMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new VaileMobileDirective();
    expect(directive).toBeTruthy();
  });
});
